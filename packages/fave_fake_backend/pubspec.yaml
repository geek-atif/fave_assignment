name: fave_fake_backend
description: The fake payment backend for the Fave take-home. Pure Dart, no Flutter dependency.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
  fake_async: ^1.3.0
